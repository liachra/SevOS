---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: sevos
# description will be included in the image's metadata
description: A custom secureblue image with usabliltiy and conviencience changes. This version is the bare bones edition, if you want extra packages that contain popular open-source and privacy respecting cloud providers please use the cloud edition

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/secureblue/kinoite-main-hardened
image-version: 43 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /
  - from-file: core/nosmt.yml
  - from-file: core/main-packages.yml
  - from-file: core/resolves.yml
  - from-file: core/trivalent.yml

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
